CREATE VIEW IF NOT EXISTS lake_view.fabletics.handpoint_transactions (
    status,
    issuerResponseText,
    issuerResponseCode,
    name,
    currency,
    amount,
    tipAmount,
    cardType,
    tenderType,
    paymentScenario,
    merchantName,
    terminalSerialNumber,
    maskedCardNumber,
    timestamp,
    protocol,
    agreementNumber,
    transactionGuid,
    approvalCode,
    customerReference,
    maskedCardToken,
    tokenizationType,
    cardTokenProvider,
    meta_create_datetime,
    meta_update_datetime
) AS
SELECT
    status,
    issuerResponseText,
    issuerResponseCode,
    name,
    currency,
    amount,
    tipAmount,
    cardType,
    tenderType,
    paymentScenario,
    merchantName,
    terminalSerialNumber,
    maskedCardNumber,
    timestamp,
    protocol,
    agreementNumber,
    transactionGuid,
    approvalCode,
    customerReference,
    maskedCardToken,
    tokenizationType,
    cardTokenProvider,
    meta_create_datetime,
    meta_update_datetime
FROM lake.fabletics.handpoint_transactions;
