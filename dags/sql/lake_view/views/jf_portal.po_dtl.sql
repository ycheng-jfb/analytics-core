CREATE VIEW IF NOT EXISTS lake_view.jf_portal.po_dtl AS
SELECT
   po_dtl_id,
   po_id,
   style,
   style_sku_seg,
   description,
   style_type,
   style_type_sku_seg,
   color_id,
   color,
   color_sku_seg,
   style_scale_dtl_id,
   size_name,
   size_sku_seg,
   case_pack_size,
   num_case_pack,
   qty,
   cost,
   misc_cost,
   duty,
   freight,
   sku,
   vendor_sku,
   user_create,
   user_update,
   date_create,
   date_update,
   ord,
   gp_status,
   style_name,
   gp_description,
   brand,
   msrp,
   vip_price,
   availability,
   subclass,
   msrpusd,
   msrpeur,
   msrpgbp,
   oracle_po_line_id,
   oracle_line_number,
   is_cancelled,
   hide,
   plm_dev_style_color_id,
   meta_create_datetime,
   meta_update_datetime
FROM
   lake.jf_portal.po_dtl
WHERE
   NOT meta_is_deleted;
