CREATE OR REPLACE VIEW data_model_3p.amz_fact_refund_line(
     order_line_id,
     order_id,
     product_key,
     order_marketplace_channel_key,
     refund_posted_local_datetime,
     item_price_local_amount,
     store_id,
     usd_conversion_rate,
     item_tax_local_amount,
     shipping_price_local_amount,
     shipping_tax_local_amount,
     shipping_discount_local_amount,
     shipping_discount_tax_local_amount,
     promotion_discount_local_amount,
     promotion_discount_tax_local_amount,
     return_shipping_local_amount,
     buyer_info_buyer_email,
     processing_status,
     fund_transfer_status,
     fund_transfer_date,
     financial_event_group_start,
     financial_event_group_end,
     meta_create_datetime,
     meta_update_datetime
)
AS
(
SELECT order_line_id,
       order_id,
       product_key,
       order_marketplace_channel_key,
       refund_posted_local_datetime,
       item_price_local_amount,
       store_id,
       usd_conversion_rate,
       item_tax_local_amount,
       shipping_price_local_amount,
       shipping_tax_local_amount,
       shipping_discount_local_amount,
       shipping_discount_tax_local_amount,
       promotion_discount_local_amount,
       promotion_discount_tax_local_amount,
       return_shipping_local_amount,
       buyer_info_buyer_email,
       processing_status,
       fund_transfer_status,
       fund_transfer_date,
       financial_event_group_start,
       financial_event_group_end,
       meta_create_datetime,
       meta_update_datetime
FROM dbt_edw_prod.stg.amz_fact_refund_line
);
