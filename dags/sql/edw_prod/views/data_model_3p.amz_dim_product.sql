CREATE OR REPLACE VIEW data_model_3p.amz_dim_product(
    product_key,
    asin,
    parent_asin,
    seller_sku,
    fulfillment_channel_sku,
    parent_title,
    item_name,
    brand,
    color,
    display_name,
    size,
    product_type,
    order_marketplace_channel_key,
    fit_type,
    special_size,
    tfg_sku,
    tfg_product_sku,
    tfg_brand,
    tfg_product_segment,
    tfg_sub_brand,
    tfg_gender,
    tfg_size,
    tfg_color_family,
    tfg_color,
    tfg_category,
    tfg_class,
    tfg_subclass,
    tfg_current_name,
    tfg_current_showroom,
    image_url,
    list_price,
    meta_create_datetime,
    meta_update_datetime
)
AS
(
SELECT product_key,
       asin,
       parent_asin,
       seller_sku,
       fulfillment_channel_sku,
       parent_title,
       item_name,
       brand,
       color,
       display_name,
       size,
       product_type,
       order_marketplace_channel_key,
       fit_type,
       special_size,
       tfg_sku,
       tfg_product_sku,
       tfg_brand,
       tfg_product_segment,
       tfg_sub_brand,
       tfg_gender,
       tfg_size,
       tfg_color_family,
       tfg_color,
       tfg_category,
       tfg_class,
       tfg_subclass,
       tfg_current_name,
       tfg_current_showroom,
       image_url,
       list_price,
       meta_create_datetime,
       meta_update_datetime
FROM dbt_edw_prod.stg.amz_dim_product
WHERE LEN(seller_sku) != 12
);
