name: airflow_jfb_cicd

on:
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  manual-approval:
    name: Manual approval gate
    runs-on: ubuntu-latest

    # 👇 this line makes GitHub show a manual "Review required" gate
    environment:
      name: manual-approval
      url: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Wait for approval
        run: echo "✅ This job will pause until manually approved in the 'manual-approval' environment."
